import React, { forwardRef } from 'react';

const Headline0 = forwardRef((props, ref) => {
    return (
        <div ref={ref}></div>
    );
});

const Headline1 = forwardRef((props, ref) => {
    return (
        <div ref={ref}></div>
    );
});

const Headline2 = forwardRef((props, ref) => {
    return (
        <div ref={ref}></div>
    );
});

const Headline3 = forwardRef((props, ref) => {
    return (
        <div ref={ref}></div>
    );
});

export { Headline0, Headline1, Headline2, Headline3 };
